# Copie en .env, ne jamais committer .env (secrets réels)

SERVER_NAME=chat.example.org

POSTGRES_USER=synapse
POSTGRES_PASSWORD=change-me

SYNAPSE_REGISTRATION_SHARED_SECRET=change-me
SYNAPSE_MACAROON_SECRET_KEY=change-me
SYNAPSE_FORM_SECRET=change-me

KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change-me
KEYCLOAK_OIDC_CLIENT_SECRET=change-me

S3_BUCKET=synapse-media
S3_ACCESS_KEY_ID=change-me
S3_SECRET_ACCESS_KEY=change-me
# format attendu : <key-id>:<base64 32 octets> ; génération : openssl rand -base64 32
MINIO_KMS_SECRET_KEY=change-me:change-me

# --- Passerelle Web Push (service de la spec 03, raccordé par REQ-INF-14) ---
# Génération des clés : pnpm --filter push-gateway exec web-push generate-vapid-keys
# VAPID_SUBJECT identifie l'émetteur auprès des services push : mailto: ou https:.
VAPID_SUBJECT=mailto:admin@example.org
VAPID_PUBLIC_KEY=change-me
VAPID_PRIVATE_KEY=change-me
# Synapse refuse ses propres requêtes sortantes vers les plages privées (protection
# SSRF). **Deux clients sortants de Synapse en dépendent, pas un seul** : la découverte
# OIDC (README.md, « login OIDC ») et — depuis le 24/08/2026 — l'appel du pusher vers
# `http://push-gateway:8008`, qui est par construction une adresse du réseau du compose.
# Laissée vide, la pile démarre, le pusher s'enregistre, l'interface annonce des
# notifications actives, et **aucun push ne part jamais** : Synapse n'appelle pas la
# passerelle, et rien ne le dit. Voir README.md, § REQ-INF-14.
# La plage est celle du réseau Docker : 172.16.0.0/12 couvre les pools par défaut. Un
# hôte configuré autrement (192.168.0.0/16) doit nommer la sienne, la plus étroite
# possible — c'est une protection qu'on entrouvre, pas qu'on retire.
SYNAPSE_IP_RANGE_WHITELIST=["172.16.0.0/12"]
# REQ-INF-09 — origine du shard (spec 11) s'il est servi ailleurs que sur SERVER_NAME.
# **Vide dans tout environnement déployé** : depuis REQ-INF-16 le proxy sert le shard sur
# le domaine du homeserver (`location /` vers le service `web` de l'overlay staging), il
# n'y a donc pas d'autre origine à ouvrir.
# À ne remplir que pour un shard lancé hors de la pile — typiquement `next dev` sur la
# machine de développement, où il faut la valeur exacte de la barre d'adresse
# (`http://localhost:3000`). Synapse compare par préfixe, et n'accepte qu'une origine en
# plus du homeserver.
# Le symptôme d'une valeur fausse est trompeur — la redirection vers Keycloak répond 302,
# le formulaire aboutit, et l'utilisateur reste bloqué sur /_synapse/client/oidc/callback.
SHARD_ORIGIN=

# --- Service de liens d'invitation (spec 12, raccordé par REQ-INF-15) ---
# Aucune variable propre : il compose son DATABASE_URL depuis POSTGRES_USER/PASSWORD
# ci-dessus, sur sa base dédiée `invite_tokens`, et joint Synapse par le réseau du
# compose. **Aucun secret d'administration ne doit apparaître ici pour lui** — la
# spec 12 lui interdit tout pouvoir Matrix, et un test de configuration le vérifie.

# --- RTC (spec 02, overlay rtc/docker-compose.yml) ---
# Le proxy et le TURN-TLS veulent tous les deux 443 : il leur faut une IP
# publique chacun. Voir rtc/README.md.
WEB_BIND_IP=
TURN_BIND_IP=
# doit correspondre à un SAN du certificat monté dans proxy/certs
TURN_DOMAIN=turn.chat.example.org
LIVEKIT_KEY=change-me
LIVEKIT_SECRET=change-me
# REQ-RTC-08 — Element Call est servi par l'overlay sous `call.${SERVER_NAME}`, sans
# variable propre : c'est le bloc `server` de rtc/call.conf qui le capte, par expression
# régulière. **Ce nom doit lui aussi être un SAN du certificat**, comme TURN_DOMAIN.
# C'est cette URL que le shard met dans NEXT_PUBLIC_ELEMENT_CALL_URL.
